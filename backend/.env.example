# Server
NODE_ENV=development
PORT=5000

# Frontend origin allowed by CORS
CLIENT_URL=http://localhost:5173

# Database (added in Step 4)
# DATABASE_URL=

# Authentication (added in Step 9)
# JWT_SECRET=